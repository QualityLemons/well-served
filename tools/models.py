"""Tools app currently has no DB models of its own.

Tool definitions live in the in-memory registry (`tools.registry`), and
persisted user submissions are tracked via the `archive.ToolInstance` model.
"""
